KETS upgraded deployment

Upload all files to the SAME Render service/repository.

Features added: live market price, price move and %, signal age/duration, estimated duration, expected move, take profit/stop loss, 06:00-18:00 EAT countdown, PWA install metadata/icons, and cache versioning.

Keep TWELVE_DATA_API_KEY and other secrets in Render Environment Variables.
Internal strategy indicators remain backend-only.
