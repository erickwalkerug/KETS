KETS SUPABASE CONNECTION

Project URL:
https://gtkfwufuenxzzivrpcrj.supabase.co

Browser/public key:
Keep SUPABASE_PUBLISHABLE_KEY only in browser-visible configuration.

Server key:
Render must have SUPABASE_SECRET_KEY (preferred modern sb_secret_* key)
or SUPABASE_SERVICE_ROLE_KEY (legacy JWT).

Never put the server key in supabase.config.js, index.html, app.js, or any
other browser-visible file.

Run KETS-main/SUPABASE_REPAIR_V2.sql in Supabase SQL Editor before deployment.
Then deploy the package and check /api/health.
